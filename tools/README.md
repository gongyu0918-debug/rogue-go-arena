# Developer Tools

Utilities in this directory are not packaged into the game runtime.

Run the card balance evaluator from the repository root:

```powershell
python tools/card_balance_eval.py --help
```
